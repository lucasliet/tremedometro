{
  "name": "Flutter",
  "image": "ghcr.io/cirruslabs/flutter:3.38.5",
  "postStartCommand": "sh -lc 'flutter --version && flutter doctor -v || true && flutter pub get && dart --disable-analytics; if command -v sdkmanager >/dev/null 2>&1; then yes | sdkmanager --licenses; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ],
      "settings": {
        "dart.flutterSdkPath": "/sdks/flutter"
      }
    }
  },
  "remoteUser": "root"
}